import { TimesDirective } from './times.directive';

describe('TimesDirective', () => {
  it('should create an instance', () => {
    const directive = new TimesDirective(null,null);
    expect(directive).toBeTruthy();
  });
});
